# 主页维护指南

README 先介绍对 AI Agent 的兴趣和常用语言 Java、Python、Go，再展示 Apache HertzBeat 等开源贡献与个人项目。不要用 Backend Developer 定义个人身份，也不要把社区项目写成个人作品。横幅使用昵称字样和抽象排版装饰，不使用动物形象；浅色与深色版本分别适配 GitHub 外观。

## 平时改哪里

| 想更新的内容 | 修改位置 |
| --- | --- |
| AI Agent 兴趣、自我介绍 | `README.md` 开头 |
| 常用语言（Java、Python、Go） | 开头的语言介绍和两张 SVG 横幅底部 |
| Agent 探索方向 | `AI agents`，保留 2–3 项 |
| 代表贡献 | `Open source` 下的 PR 列表 |
| 新的个人项目 | `Personal lab` 下，复制现有项目的格式 |
| 邮箱或其他联系入口 | `Say hello` |
| 横幅文字、配色或图形 | `assets/profile-banner.svg` 和 `assets/profile-banner-dark.svg` |

## 新增作品

将下面的格式复制到 `Personal lab`，替换所有尖括号里的内容后再发布。主页建议保留 2–3 个最有代表性的项目。

```markdown
### [<项目名称>](<仓库或演示地址>)

**<你的角色> · <当前阶段>** / <技术或领域>

<一两句话说明解决什么问题、你具体做了什么。>

[Explore the code ↗](<仓库地址>) · [Try the demo ↗](<演示地址>)
```

只有代码时删掉 demo 入口。参与社区项目时标明 contributor、maintainer 等实际角色；仅 fork 的仓库不直接作为自己的作品展示。

## 新增贡献

每条写成「具体改动 + 作用 + 已合并 PR 链接」，保留 3 条左右。旧贡献可以用 `More HertzBeat contributions` 搜索入口查看，避免把主页变成提交日志。

```markdown
- **<改动主题>** — <具体做了什么、有什么用>。 [PR #<编号> ↗](<PR 地址>)
```

## 以后展示文章、演讲或其他内容

有真实内容时，在个人项目之后、联系信息之前新增 `## Notes & talks`，用「标题 + 一句说明 + 原文、视频或演示链接」展示。也可以继续在 `Personal lab` 下添加个人作品。只放已发布或可访问的内容，不必提前创建空栏目。

## 发布前

- 确认项目角色、阶段和链接准确。
- 修改横幅时同时更新浅色、深色 SVG；昵称和重要定位也要保留在正文中，便于手机阅读和辅助技术读取。
- 用 GitHub 的预览检查标题、表格和图片。文件使用仓库相对路径即可，无需图床、定时任务或密钥。
